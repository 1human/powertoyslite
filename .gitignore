# apply-lite-patch.ps1 每次运行都会重写这个构建清单，属于生成物
lite/lite.build.json

# 本地构建 / 调试
build/
artifacts/
*.binlog
*.log

# 校验工作流时抽取内嵌脚本用的临时目录（属于生成物，绝不能入库）
.tmp-steps/
.tmp-*

# 编辑器 / IDE 的本地状态，不入库（会随每次 IDE 操作变化，污染 diff）
.idea/
.vs/
*.user
